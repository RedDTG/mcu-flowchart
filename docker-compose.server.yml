services:
  api:
    image: ${API_IMAGE}
    container_name: mcu-flowchart-api
    restart: unless-stopped
    ports:
      - "8001:8001"

  web:
    image: ${WEB_IMAGE}
    container_name: mcu-flowchart-web
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "3001:3001"
